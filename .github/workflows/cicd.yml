name: Test & build

on:
  push:

jobs:
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-go@v6
      - name: Build
        run: make build
      - name: Test
        run: make test
